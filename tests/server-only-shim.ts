/*
 * Vitest executes server modules in a Node test process rather than through
 * the Next.js compiler. This empty module replaces the server-only poison
 * pill in tests only. Production resolution is not affected.
 */
export {};
